"""TrafficPulse package root.

This is the minimal, importable package root created in Phase 0-F unit U1.
It intentionally contains no runtime behaviour and no ML/CV dependencies —
its only responsibility at this stage is to be importable and to expose a
package version for smoke testing. Domain contracts (the ``contracts``
package) first appear in U2; other modules appear only when a unit needs them.
"""

__version__ = "1.0.0"

__all__ = ["__version__"]
